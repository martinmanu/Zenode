/**
 * Builds an SVG path for a rectangle with independent corner radii.
 */
export declare function roundedRectPath(x: number, y: number, width: number, height: number, r1: number, r2: number, r3: number, r4: number): string;
